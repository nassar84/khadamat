# دليل اختبار Posts و Comments

## 📋 المتطلبات الأساسية
1. تشغيل الـ Backend: `dotnet run --project src\Khadamat.WebAPI`
2. تشغيل الـ Frontend: `dotnet run --project src\Khadamat.WasmHost --urls http://localhost:5028`
3. حساب مزود خدمة موثق: `user1@khadamat.com` / `User@123`

## 🧪 اختبار Posts (المنشورات)

### الخطوة 1: تسجيل الدخول
1. افتح http://localhost:5028/login
2. أدخل البيانات:
   - Email: user1@khadamat.com
   - Password: User@123
3. اضغط "تسجيل الدخول"

### الخطوة 2: الانتقال لخدماتك
1. من القائمة الجانبية → "خدماتي"
2. اختر أي خدمة من القائمة
3. اضغط على زر "عرض" أو على الخدمة نفسها

### الخطوة 3: إضافة منشور
1. في صفحة تفاصيل الخدمة، اختر تبويب **"المنشورات"**
2. ستجد حقل نص في الأعلى (إذا كنت مالك الخدمة)
3. اكتب منشور مثل:
   ```
   🎉 عرض خاص: خصم 20% على جميع الخدمات لفترة محدودة!
   📞 للحجز: 01012345678
   ```
4. (اختياري) أضف صورة عبر زر الصورة
5. اضغط "نشر"

### الخطوة 4: التحقق
- يجب أن يظهر المنشور فوراً في قائمة المنشورات
- يجب أن يحتوي على: النص، الصورة (إن وجدت), التاريخ، عدد الإعجابات (0)

## 💬 اختبار Comments (التعليقات)

### الخطوة 1: عرض منشور
1. في نفس صفحة المنشورات، اختر أي منشور موجود
2. ابحث عن زر "تعليق" أو حقل التعليق أسفل المنشور

### الخطوة 2: إضافة تعليق
1. اكتب تعليق مثل:
   ```
   عرض ممتاز! متى ينتهي العرض؟ 🤔
   ```
2. اضغط "إرسال" أو "تعليق"

### الخطوة 3: التحقق
- يجب أن يظهر التعليق فوراً أسفل المنشور
- يجب أن يحتوي على: اسم المستخدم، النص، التاريخ

## 🔍 اختبار API مباشر (للمطورين)

### إنشاء Post
```powershell
$headers = @{
    "Content-Type" = "application/json"
    "Authorization" = "Bearer YOUR_TOKEN_HERE"
}
$body = @{
    Content = "عرض خاص: خصم 20%!"
    ImageUrl = $null
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:5144/api/v1/posts" -Method POST -Headers $headers -Body $body
```

### إنشاء Comment
```powershell
$headers = @{
    "Content-Type" = "application/json"
    "Authorization" = "Bearer YOUR_TOKEN_HERE"
}
$body = @{
    PostId = 1
    Text = "تعليق تجريبي رائع!"
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:5144/api/v1/comments" -Method POST -Headers $headers -Body $body
```

## 📊 النتائج المتوقعة

### ✅ Posts ناجحة إذا:
- ظهر المنشور في القائمة فوراً
- يحتوي على النص الصحيح
- يظهر اسم المزود وصورته
- التاريخ صحيح

### ✅ Comments ناجحة إذا:
- ظهر التعليق أسفل المنشور
- يحتوي على النص الصحيح
- يظهر اسم المعلق
- يمكن حذفه من قبل صاحبه

## 🐛 استكشاف الأخطاء

### المشكلة: "لا يظهر حقل إضافة منشور"
**الحل:** تأكد من:
- أنك مسجل دخول كمزود خدمة موثق
- أنك في صفحة خدمة تملكها أنت
- أن الـ UI يتعرف عليك كـ Provider

### المشكلة: "خطأ 401 Unauthorized"
**الحل:**
- تأكد من تسجيل الدخول
- تحقق من صلاحية الـ Token
- تأكد من أن الحساب موثق (IsVerified = true)

### المشكلة: "خطأ 403 Forbidden"
**الحل:**
- Posts تتطلب حساب Provider
- تأكد من أن حسابك لديه صلاحية RequireProvider

## 📝 ملاحظات مهمة

1. **Posts** يمكن إنشاؤها فقط من قبل مزودي الخدمات الموثقين
2. **Comments** يمكن إضافتها من أي مستخدم مسجل
3. كل منشور مرتبط بـ ProviderId (وليس ServiceId)
4. التعليقات مرتبطة بـ PostId
5. يمكن حذف المنشور/التعليق من قبل صاحبه أو الـ Admin

## 🎯 الخطوات التالية

بعد نجاح الاختبار:
1. ✅ تأكد من عمل الإشعارات عند إضافة تعليق
2. ✅ اختبر نظام الإعجابات (Likes)
3. ✅ اختبر حذف المنشورات والتعليقات
4. ✅ اختبر الصلاحيات (من يمكنه الحذف/التعديل)
