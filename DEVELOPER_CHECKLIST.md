# ✅ Khadamat - Checklist للمطورين

> **استخدم هذه القائمة قبل كل Commit أو Pull Request**

---

## 📋 قبل إضافة/تعديل API Method

- [ ] هل تم إضافة `try-catch` block؟
- [ ] هل يتم إرجاع قيمة افتراضية آمنة (`new List<>()` أو `null`) عند الفشل؟
- [ ] هل تم تسجيل الخطأ في Console؟
- [ ] هل تم اختبار الـ method مع:
  - [ ] بيانات صحيحة
  - [ ] بيانات فارغة
  - [ ] فشل الاتصال بالـ API

**مثال صحيح**:
```csharp
public async Task<List<T>> GetDataAsync()
{
    try
    {
        var response = await _http.GetFromJsonAsync<ApiResponse<List<T>>>(url);
        return response?.Data ?? new List<T>();
    }
    catch (Exception ex)
    {
        Console.WriteLine($"Error: {ex.Message}");
        return new List<T>();
    }
}
```

---

## 📋 قبل إضافة صفحة جديدة

### صفحات Admin (`/admin/*`)
- [ ] استخدام `@layout Khadamat.BlazorUI.Layout.AdminLayout`
- [ ] إضافة `@attribute [Authorize(Roles = "SystemAdmin,SuperAdmin")]`
- [ ] التأكد من ظهور الشريط الجانبي الإداري
- [ ] اختبار الصفحة مع System Admin و Super Admin

### صفحات العملاء
- [ ] استخدام `@layout Khadamat.BlazorUI.Layout.MainLayout`
- [ ] التأكد من ظهور الشريط الجانبي الرئيسي
- [ ] اختبار الصفحة مع مستخدم عادي ومزود خدمة

---

## 📋 عند عرض قوائم بيانات

- [ ] التعامل مع حالة `null`:
  ```razor
  @if (items != null)
  {
      <!-- عرض البيانات -->
  }
  else
  {
      <LoadingSpinner />
  }
  ```

- [ ] التعامل مع القائمة الفارغة:
  ```razor
  @if (items.Any())
  {
      @foreach (var item in items)
      {
          <!-- عرض العنصر -->
      }
  }
  else
  {
      <div class="empty-state">
          <i class="fa-solid fa-folder-open"></i>
          <p>لا توجد بيانات</p>
      </div>
  }
  ```

- [ ] عرض رسالة واضحة للمستخدم عند عدم وجود بيانات
- [ ] عرض Loading Spinner أثناء التحميل

---

## 📋 عند عرض معلومات المستخدم

- [ ] فحص الأدوار بالترتيب الصحيح (من الأعلى للأدنى):
  ```razor
  @(context.User.IsInRole("SuperAdmin") ? "مدير خارق" : 
    context.User.IsInRole("SystemAdmin") ? "مدير نظام" : 
    State.IsProvider ? "مزود خدمة" : "عميل")
  ```

- [ ] اختبار العرض مع كل دور:
  - [ ] Super Admin
  - [ ] System Admin
  - [ ] مزود خدمة
  - [ ] مستخدم عادي

---

## 📋 عند استخدام LocationSelector

- [ ] التأكد من تمرير `Governorates` list
- [ ] استخدام `@bind-CityId` للربط الثنائي
- [ ] التأكد من تحميل المدن عند اختيار المحافظة
- [ ] اختبار مع:
  - [ ] محافظة بها مدن
  - [ ] محافظة فارغة
  - [ ] تغيير المحافظة

---

## 📋 قبل الـ Commit

- [ ] تم اختبار جميع التغييرات محلياً
- [ ] لا توجد أخطاء في Build
- [ ] تم اختبار الميزة مع بيانات حقيقية
- [ ] تم اختبار الميزة مع بيانات فارغة
- [ ] تم مراجعة `ARCHITECTURE_DECISIONS.md`
- [ ] تم التأكد من عدم كسر ميزات موجودة

---

## 📋 الأخطاء الشائعة - تحقق منها!

### ❌ نسيان Error Handling
```csharp
// خطأ
var data = await _http.GetFromJsonAsync<T>(url);
```

### ❌ عدم فحص null
```razor
<!-- خطأ -->
@foreach (var item in list)
```

### ❌ استخدام Layout خاطئ
```razor
<!-- خطأ في صفحة Admin -->
@layout MainLayout
```

### ❌ عدم عرض Empty State
```razor
<!-- خطأ -->
@foreach (var item in items)
{
    <!-- فقط عرض البيانات -->
}
```

---

## 🚀 اختبارات سريعة

### اختبار الشريط الجانبي
```
1. تسجيل دخول كـ Admin
2. فتح /admin/users
3. ✅ يجب ظهور الشريط الجانبي الإداري
```

### اختبار عرض الدور
```
1. تسجيل دخول كـ System Admin
2. النظر للشريط العلوي
3. ✅ يجب عرض "مدير نظام"
```

### اختبار تحميل المدن
```
1. فتح تعديل مستخدم
2. اختيار محافظة
3. ✅ يجب تحميل المدن تلقائياً
```

---

## 📞 عند مواجهة مشكلة

1. راجع `ARCHITECTURE_DECISIONS.md`
2. تحقق من هذه القائمة
3. ابحث عن مشكلة مشابهة تم حلها سابقاً
4. اختبر مع بيانات مختلفة (فارغة، null، صحيحة)

---

**تذكر**: هذه القائمة موجودة لمساعدتك على تجنب الأخطاء الشائعة!
