@page "/client/notifications"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the unified notifications page
        Navigation.NavigateTo("/user/notifications", replace: true);
    }
}
