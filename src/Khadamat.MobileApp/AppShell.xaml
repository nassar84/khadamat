<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Khadamat.MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Khadamat.MobileApp.Views"
    xmlns:viewModels="clr-namespace:Khadamat.MobileApp.ViewModels"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:DataType="viewModels:ShellViewModel"
    Title="{Binding AppName}"
    FlyoutBackgroundColor="#ffffff"
    FlyoutBehavior="Flyout"
    FlowDirection="RightToLeft">

    <Shell.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <toolkit:BoolToObjectConverter x:Key="BooleanToColorConverter" 
                                           TrueObject="#6366f1" 
                                           FalseObject="#94a3b8" />

            <Style TargetType="Label" Class="FlyoutItemLabel">
                <Setter Property="TextColor" Value="#0f172a" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="Margin" Value="10,0" />
            </Style>

            <!-- TabBar Styling for Glassmorphism-ish look -->
            <Style TargetType="TabBar">
                <Setter Property="Shell.TabBarBackgroundColor" Value="#F8FAFC" />
                <Setter Property="Shell.TabBarForegroundColor" Value="{DynamicResource Primary}" />
                <Setter Property="Shell.TabBarTitleColor" Value="{DynamicResource Primary}" />
                <Setter Property="Shell.TabBarUnselectedColor" Value="#94a3b8" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <Shell.ItemTemplate>
        <DataTemplate x:DataType="BaseShellItem">
            <Grid HeightRequest="50" Padding="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Source="{Binding Icon}" HeightRequest="20" WidthRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                <Label Grid.Column="1" Text="{Binding Title}" StyleClass="FlyoutItemLabel" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <Shell.MenuItemTemplate>
        <DataTemplate x:DataType="MenuItem">
            <Grid HeightRequest="50" Padding="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Source="{Binding IconImageSource}" HeightRequest="20" WidthRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                <Label Grid.Column="1" Text="{Binding Text}" StyleClass="FlyoutItemLabel" />
            </Grid>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    <Shell.TitleView>
        <Grid Padding="10,0" VerticalOptions="Fill" FlowDirection="RightToLeft">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <HorizontalStackLayout Grid.Column="0" HorizontalOptions="Start" VerticalOptions="Center" Spacing="8">
                <HorizontalStackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="//HomePage" />
                </HorizontalStackLayout.GestureRecognizers>
                <Image Source="{Binding AppLogo}" HeightRequest="28" WidthRequest="28" Aspect="AspectFit"/>
                <Label Text="{Binding AppName}" FontSize="18" FontAttributes="Bold" TextColor="White" VerticalOptions="Center"/>
            </HorizontalStackLayout>
            
            <ImageButton Grid.Column="2" Source="{FontImageSource FontFamily=OpenSansRegular, Glyph='&#x1F3A8;', Color=White}"
                         HeightRequest="30" WidthRequest="30" Margin="5,0"
                         Command="{Binding ShowThemePickerCommand}" />

            <ImageButton Grid.Column="3" Source="{FontImageSource FontFamily=OpenSansRegular, Glyph='&#x1F514;', Color=White}"
                         HeightRequest="30" WidthRequest="30" Margin="5,0"
                         IsVisible="{Binding IsAuthenticated}"
                         Command="{Binding NavigateCommand}" CommandParameter="notifications" />
            
            <ImageButton Grid.Column="4" Source="{FontImageSource FontFamily=OpenSansRegular, Glyph='&#x1F50D;', Color=White}"
                         HeightRequest="30" WidthRequest="30" Margin="5,0"
                         Command="{Binding NavigateCommand}" CommandParameter="explore" />
        </Grid>
    </Shell.TitleView>

    <!-- Full Custom Flyout Content -->
    <Shell.FlyoutContent>
        <ScrollView BackgroundColor="White" FlowDirection="RightToLeft">
            <Grid RowDefinitions="Auto,*,Auto" MinimumHeightRequest="600">

                <!-- ═══ TOP HEADER ═══ -->
                <Grid Grid.Row="0">

                    <!-- GUEST Header: White bg, logo + app name -->
                    <VerticalStackLayout IsVisible="{Binding IsNotAuthenticated}" Padding="20,50,20,10" Spacing="0">
                        <!-- Logo + App Name -->
                        <HorizontalStackLayout Spacing="10" VerticalOptions="Center" HorizontalOptions="Start">
                            <HorizontalStackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="home" />
                            </HorizontalStackLayout.GestureRecognizers>
                            <Label Text="⚡" FontSize="26" TextColor="{DynamicResource Primary}" VerticalOptions="Center" />
                            <Label Text="{Binding AppName}" FontSize="24" FontAttributes="Bold"
                                   TextColor="{DynamicResource Primary}" VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <BoxView HeightRequest="1" Color="#f1f5f9" Margin="0,20,0,0" />
                        <!-- Section label -->
                        <Label Text="{Binding AppName, StringFormat='إكتشف {0}'}"
                               FontSize="12" TextColor="#94a3b8" Margin="0,16,0,10" />
                        <!-- Home Button (filled, active) -->
                        <Border BackgroundColor="{DynamicResource Primary}" StrokeThickness="0" Margin="0,0,0,8">
                            <Border.StrokeShape><RoundRectangle CornerRadius="12" /></Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="home" />
                            </Border.GestureRecognizers>
                            <Grid Padding="16,13" ColumnDefinitions="*,Auto">
                                <Label Text="الرئيسية" TextColor="White" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" />
                                <Label Grid.Column="1" Text="🏠" FontSize="18" VerticalOptions="Center" />
                            </Grid>
                        </Border>
                        <!-- Refresh -->
                        <Grid Padding="8,10" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding RefreshCommand}" />
                            </Grid.GestureRecognizers>
                            <Label Text="تحديث التطبيق" TextColor="#334155" FontSize="14" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="🔄" FontSize="16" VerticalOptions="Center" />
                        </Grid>
                    </VerticalStackLayout>

                    <!-- AUTHENTICATED Header: Purple gradient -->
                    <Grid IsVisible="{Binding IsAuthenticated}">
                        <!-- Purple Gradient Background -->
                        <Border StrokeThickness="0">
                            <Border.StrokeShape><RoundRectangle CornerRadius="0,0,0,32" /></Border.StrokeShape>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="{DynamicResource Primary}" Offset="0.0" />
                                    <GradientStop Color="#a855f7" Offset="1.0" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentView HeightRequest="140" />
                        </Border>

                        <!-- Overlay Content -->
                        <Grid Padding="20,50,20,20" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="profile" />
                            </Grid.GestureRecognizers>
                            <!-- Name + Welcome -->
                            <VerticalStackLayout VerticalOptions="Center" Spacing="4">
                                <Label Text="مرحباً بك،" TextColor="White" FontSize="13" Opacity="0.85" />
                                <Label Text="{Binding UserName}" TextColor="White" FontSize="18" FontAttributes="Bold" />
                            </VerticalStackLayout>
                            <!-- Avatar (square rounded) -->
                            <Border Grid.Column="1" WidthRequest="62" HeightRequest="62"
                                    StrokeThickness="3" Stroke="White">
                                <Border.StrokeShape><RoundRectangle CornerRadius="14" /></Border.StrokeShape>
                                <Border.Shadow><Shadow Brush="#000000" Offset="0,6" Radius="16" Opacity="0.2" /></Border.Shadow>
                                <Image Source="{Binding UserImage}" Aspect="AspectFill" />
                            </Border>
                        </Grid>
                    </Grid>

                </Grid>

                <!-- ═══ MIDDLE CONTENT ═══ -->
                <VerticalStackLayout Grid.Row="1" Padding="20,0" Spacing="0">

                    <!-- ──── GUEST: Join Card ──── -->
                    <Border IsVisible="{Binding IsNotAuthenticated}"
                            BackgroundColor="White" StrokeThickness="1" Stroke="#f1f5f9"
                            Padding="20" Margin="0,8,0,0">
                        <Border.StrokeShape><RoundRectangle CornerRadius="16" /></Border.StrokeShape>
                        <Border.Shadow><Shadow Brush="#000000" Offset="0,4" Radius="12" Opacity="0.06" /></Border.Shadow>
                        <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                            <Border BackgroundColor="#f3f0ff" StrokeThickness="0" WidthRequest="60" HeightRequest="60" HorizontalOptions="Center">
                                <Border.StrokeShape><Ellipse /></Border.StrokeShape>
                                <Label Text="👤" FontSize="26" HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            <Label Text="انضم إلينا الآن" FontSize="18" FontAttributes="Bold" TextColor="#0f172a" HorizontalOptions="Center" />
                            <Label Text="سجل دخولك لتتمكن من طلب الخدمات والتواصل مع المزودين"
                                   FontSize="12" TextColor="#64748b" HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center" LineHeight="1.5" />
                            <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,4,0,0">
                                <Border Grid.Column="0" BackgroundColor="{DynamicResource Primary}" StrokeThickness="0">
                                    <Border.StrokeShape><RoundRectangle CornerRadius="10" /></Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="login" />
                                    </Border.GestureRecognizers>
                                    <Label Text="دخول" TextColor="White" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" Padding="0,12" />
                                </Border>
                                <Border Grid.Column="1" BackgroundColor="White" StrokeThickness="1.5" Stroke="{DynamicResource Primary}">
                                    <Border.StrokeShape><RoundRectangle CornerRadius="10" /></Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="register" />
                                    </Border.GestureRecognizers>
                                    <Label Text="حساب جديد" TextColor="{DynamicResource Primary}" FontAttributes="Bold" FontSize="13" HorizontalOptions="Center" Padding="0,12" />
                                </Border>
                            </Grid>
                        </VerticalStackLayout>
                    </Border>

                    <!-- ──── AUTHENTICATED: Main Menu + Activity ──── -->
                    <VerticalStackLayout IsVisible="{Binding IsAuthenticated}" Spacing="0" Margin="0,16,0,0">

                        <!-- Section: القائمة الرئيسية -->
                        <Label Text="القائمة الرئيسية" FontSize="12" TextColor="#94a3b8" FontAttributes="Bold" Margin="8,0,8,8" />

                        <!-- Home (active) -->
                        <Border BackgroundColor="{DynamicResource Primary}" StrokeThickness="0" Margin="0,0,0,8">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14" /></Border.StrokeShape>
                            <Border.Shadow><Shadow Brush="{DynamicResource Primary}" Offset="0,8" Radius="20" Opacity="0.3" /></Border.Shadow>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="home" />
                            </Border.GestureRecognizers>
                            <Grid Padding="16,14" ColumnDefinitions="*,Auto">
                                <Label Text="الرئيسية" TextColor="White" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" />
                                <Label Grid.Column="1" Text="🏠" FontSize="18" VerticalOptions="Center" />
                            </Grid>
                        </Border>

                        <!-- Sub-label: التصنيفات -->
                        <Label Text="التصنيفات" FontSize="12" TextColor="#94a3b8" FontAttributes="Bold" Margin="8,8,8,6" />

                        <!-- المتجر + مميز -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto,Auto" BackgroundColor="#fafbff" Margin="0,0,0,2">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="marketplace" />
                            </Grid.GestureRecognizers>
                            <Label Text="المتجر" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <!-- مميز Badge -->
                            <Border Grid.Column="1" BackgroundColor="#f59e0b" StrokeThickness="0" Margin="0,0,8,0">
                                <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                                <Label Text="مميز" TextColor="White" FontSize="10" FontAttributes="Bold" Padding="8,3" />
                            </Border>
                            <Label Grid.Column="2" Text="🛍️" FontSize="18" VerticalOptions="Center" />
                        </Grid>

                        <BoxView HeightRequest="1" Color="#f1f5f9" Margin="0,4,0,4" />

                        <!-- Refresh -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding RefreshCommand}" />
                            </Grid.GestureRecognizers>
                            <Label Text="تحديث التطبيق" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="🔄" FontSize="18" VerticalOptions="Center" />
                        </Grid>

                        <BoxView HeightRequest="8" Color="#f8fafc" Margin="0,8,0,8" />

                        <!-- Section: نشاطي -->
                        <Label Text="نشاطي" FontSize="12" TextColor="#94a3b8" FontAttributes="Bold" Margin="8,0,8,8" />

                        <!-- خدماتي -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="my-services" />
                            </Grid.GestureRecognizers>
                            <Label Text="خدماتي" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="💼" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- طلباتي -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="services" />
                            </Grid.GestureRecognizers>
                            <Label Text="طلباتي" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="📋" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- رسائلي -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="messages" />
                            </Grid.GestureRecognizers>
                            <Label Text="رسائلي" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="✉️" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- الإشعارات -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="notifications" />
                            </Grid.GestureRecognizers>
                            <Label Text="الإشعارات" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="🔔" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- المفضلة -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="favorites" />
                            </Grid.GestureRecognizers>
                            <Label Text="المفضلة" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="❤️" FontSize="18" VerticalOptions="Center" TextColor="#ef4444" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- إحصائيات إعلاناتي -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="my-services" />
                            </Grid.GestureRecognizers>
                            <Label Text="إحصائيات إعلاناتي" TextColor="#92400e" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Border Grid.Column="1" BackgroundColor="#f59e0b" StrokeThickness="0" Margin="0,0,8,0">
                                <Border.StrokeShape><RoundRectangle CornerRadius="6" /></Border.StrokeShape>
                                <Label Text="Ad" TextColor="White" FontSize="9" FontAttributes="Bold" Padding="5,2" />
                            </Border>
                            <Label Grid.Column="2" Text="📊" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- إعلاناتي -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="my-services" />
                            </Grid.GestureRecognizers>
                            <Label Text="إعلاناتي" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="📦" FontSize="18" VerticalOptions="Center" TextColor="#8b5cf6" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- Admin -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto" IsVisible="{Binding IsAdmin}">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="admin" />
                            </Grid.GestureRecognizers>
                            <Label Text="لوحة المدير" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="🛡️" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" IsVisible="{Binding IsAdmin}" />

                        <!-- Settings -->
                        <Grid Padding="12,13" ColumnDefinitions="*,Auto">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="settings" />
                            </Grid.GestureRecognizers>
                            <Label Text="الإعدادات" TextColor="#1e293b" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="⚙️" FontSize="18" VerticalOptions="Center" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#f1f5f9" />

                        <!-- Logout -->
                        <Grid Padding="12,14" ColumnDefinitions="*,Auto" Margin="0,8,0,0" BackgroundColor="#fff1f2">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="logout" />
                            </Grid.GestureRecognizers>
                            <Label Text="تسجيل الخروج" TextColor="#e11d48" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="🚪" FontSize="18" VerticalOptions="Center" />
                        </Grid>

                    </VerticalStackLayout>

                </VerticalStackLayout>

                <!-- ═══ FOOTER ═══ -->
                <VerticalStackLayout Grid.Row="2" Padding="20,16,20,30" Spacing="12">
                    <BoxView HeightRequest="1" Color="#f1f5f9" />
                    <!-- Share App -->
                    <Grid ColumnDefinitions="*,Auto" Padding="4,8">
                        <Label Text="شارك التطبيق" TextColor="{DynamicResource Primary}" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" />
                        <Label Grid.Column="1" Text="🔗" FontSize="16" VerticalOptions="Center" />
                    </Grid>
                    <!-- Terms / Privacy -->
                    <HorizontalStackLayout Spacing="16" HorizontalOptions="Center">
                        <Label TextColor="#64748b" FontSize="12">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="terms" />
                            </Label.GestureRecognizers>
                            <Label.FormattedText>
                                <FormattedString><Span Text="الشروط والأحكام" TextDecorations="Underline" /></FormattedString>
                            </Label.FormattedText>
                        </Label>
                        <Label Text="|" TextColor="#cbd5e1" FontSize="12" VerticalOptions="Center" />
                        <Label TextColor="#64748b" FontSize="12">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="terms" />
                            </Label.GestureRecognizers>
                            <Label.FormattedText>
                                <FormattedString><Span Text="الخصوصية" TextDecorations="Underline" /></FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </HorizontalStackLayout>
                    <Label Text="إصدار 1.0.0" FontSize="11" TextColor="#94a3b8" HorizontalOptions="Center" />
                </VerticalStackLayout>

            </Grid>
        </ScrollView>
    </Shell.FlyoutContent>



    <TabBar x:Name="MainTabBar">
        <!-- 1. Home (house) - \uf015 (Prominent style in MAUI via contrast) -->
        <Tab Title="الرئيسية" Route="HomePage">
            <Tab.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F3E0;" Color="#6366f1" /></Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:HomePage}" />
        </Tab>

        <!-- 2. Categories (th-large) - \uf009 -->
        <Tab Title="الأقسام" Route="CategoriesPage" IsVisible="{Binding IsClientMode}">
            <Tab.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4C2;" Color="{Binding IsClientMode, Converter={StaticResource BooleanToColorConverter}}" /></Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:CategoriesPage}" />
        </Tab>

        <!-- 3. Marketplace (store) - \uf54e -->
        <Tab Title="المتجر" Route="MarketplacePage" IsVisible="{Binding IsClientMode}">
            <Tab.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F6CD;" Color="#f59e0b" /></Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:MarketplacePage}" />
        </Tab>

        <!-- 4. Client: Favorites (heart) - \uf004 | Provider: My Services (briefcase) - \uf0b1 -->
        <Tab Title="المفضلة" Route="FavoritesPage" IsVisible="{Binding IsClientMode}">
            <Tab.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x2764;" Color="#ef4444" /></Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:FavoritesPage}" />
        </Tab>
        
        <Tab Title="خدماتي" Route="MyServicesPage" IsVisible="{Binding IsProviderMode}">
            <Tab.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4BC;" Color="#6366f1" /></Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:MyServicesPage}" />
        </Tab>

        <!-- 5. Profile/Account -->
        <Tab Title="{Binding UserTitle}" Route="ProfileTab">
            <Tab.Icon>
                <FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F464;" Color="#64748b" />
            </Tab.Icon>
            <ShellContent Title="الحساب" ContentTemplate="{DataTemplate views:ProfilePage}" IsVisible="{Binding IsAuthenticated}" />
            <ShellContent Title="دخول" ContentTemplate="{DataTemplate views:LoginPage}" IsVisible="{Binding IsNotAuthenticated}" />
        </Tab>
    </TabBar>

    <!-- Side Menu (Flyout) Navigation Groups -->
    
    <!-- ==== الاستكشاف ==== -->
    <FlyoutItem Title="الرئيسية" Route="flyout_home" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F3E0;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:HomePage}" />
    </FlyoutItem>

    <FlyoutItem Title="التصنيفات" Route="flyout_categories" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4C2;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:CategoriesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="المتجر" Route="flyout_marketplace" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F6CD;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:MarketplacePage}" />
    </FlyoutItem>

    <FlyoutItem Title="أضف إعلان" Route="flyout_provider/apply" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x2795;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:PostServicePage}" />
    </FlyoutItem>

    <!-- ==== نشاطاتي (Visible Only if Authenticated) ==== -->
    <FlyoutItem Title="إعلاناتي" Route="flyout_my-services" IsVisible="{Binding IsAuthenticated}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4BC;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:MyServicesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="خدماتي" Route="flyout_services" IsVisible="{Binding IsAuthenticated}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x2705;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:ServicesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="المفضلة" Route="flyout_favorites" IsVisible="{Binding IsAuthenticated}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x2764;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:FavoritesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="الرسائل" Route="flyout_messages" IsVisible="{Binding IsAuthenticated}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4AC;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:MessagesPage}" />
    </FlyoutItem>

    <!-- ==== الإدارة (Visible Only if Admin) ==== -->
    <FlyoutItem Title="لوحة المدير" Route="flyout_admin" IsVisible="{Binding IsAdmin}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F6E1;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:AdminPage}" />
    </FlyoutItem>

    <FlyoutItem Title="إدارة الإعلانات" Route="flyout_admin/ads" IsVisible="{Binding IsAdmin}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4DC;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:AdminAdsPage}" />
    </FlyoutItem>

    <!-- Visual Separator -->
    <MenuItem Text="━━━━━━━━━━━━" IsEnabled="False" />

    <!-- ==== النظام ==== -->
    <FlyoutItem Title="الإعدادات" Route="flyout_settings" IsVisible="{Binding IsAuthenticated}" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x2699;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="الدعم والمساعدة" Route="flyout_support" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F198;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:SupportPage}" />
    </FlyoutItem>

    <FlyoutItem Title="الشروط والأحكام" Route="flyout_terms" FlyoutDisplayOptions="AsSingleItem">
        <FlyoutItem.Icon><FontImageSource FontFamily="OpenSansRegular" Glyph="&#x1F4DC;" Color="#0f172a" /></FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:TermsPage}" />
    </FlyoutItem>

    <MenuItem Text="تحديث التطبيق" Command="{Binding RefreshCommand}" IconImageSource="{FontImageSource FontFamily=OpenSansRegular, Glyph='&#x1f504;', Color='#0f172a'}" />
    
    <!-- Logout is in FlyoutFooter -->

</Shell>
